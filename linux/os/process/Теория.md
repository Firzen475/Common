# Процессы
* Отдельная программа для которой выделено место в адресном пространстве.
* На каждый процесс выделяется процессорное время.
  * Управляется приоритетом.

### Состояния процессов в Linux

| Символ | Состояние                  | Описание                                                                 | Как возникает                          |
|--------|----------------------------|--------------------------------------------------------------------------|----------------------------------------|
| **R**  | Running (Выполняемый)      | Процесс использует CPU или в очереди планировщика                        | Активное выполнение кода              |
| **S**  | Interruptible Sleep        | Ожидает событие (можно разбудить сигналом)                               | `read()`, `sleep()`, ожидание ввода   |
| **D**  | Uninterruptible Sleep      | Ждет аппаратное событие (не реагирует на сигналы)                        | Дисковые I/O операции                 |
| **T**  | Stopped (Остановлен)       | Приостановлен сигналом                                                  | `Ctrl+Z` или `kill -STOP`             |
| **Z**  | Zombie (Зомби)             | Завершился, но статус не прочитан родителем                              | `exit()` без `wait()`                 |
| **X**  | Dead (Мёртвый)             | Полностью завершен (кратковременное состояние)                           | После обработки `wait()`              |

### Дополнительные флаги состояний

| Флаг  | Значение                          |
|-------|-----------------------------------|
| **<** | Высокий приоритет (nice < 0)      |
| **N** | Низкий приоритет (nice > 0)       |
| **+** | Процесс в foreground-группе       |
| **s** | Лидер сессии                      |
| **l** | Многопоточный процесс             |



### **Зомби-процессы (Zombie) в Linux**

| **Характеристика**       | **Описание**                                                                 |
|--------------------------|-----------------------------------------------------------------------------|
| **Что такое?**           | Завершённый процесс, чей статус не прочитан родителем (`wait()` не вызван). |
| **Обозначение**          | `Z` или `<defunct>` в `ps`, `top`.                                         |
| **Потребляют ресурсы?**  | ❌ Нет (не используют CPU, RAM, только PID).                               |
| **Причина появления**    | Родительский процесс не обработал завершение потомка.                      |
| **Чем опасно?**          | Накопление зомби → истощение PIDs → новые процессы не запускаются.         |
| **Убийство зомби**       | `kill -9 PPID_зомби` (убить родителя → зомби усыновит `init` и очистится). |

```
  +---------------------+       +---------------------+
  |   Родительский      |       |    Дочерний         |
  |     процесс         |       |     процесс         |
  +----------+----------+       +----------+----------+
            |                             |
            | fork()                      |
            +---------------------------> |
            |                             |
            |         exec() (опционально)|
            |                             |
            | <--+                        |
            |    |      Работают          |
            |    |      параллельно       |
            |    |                        |
            |    | exit() или сигнал      |
            |    |                        |
            |    +---------------------+  |
            |                          |  |
            | wait()/waitpid()         |  |
            | (сбор статуса)           |  |
            |                          |  |
            | <------------------------+  |
            |                             |
            |  Зомби (если wait не вызван)|
            |                             |
  +----------+----------+       +----------+----------+
  |                     |       |                     |
  |  Процесс завершён   |       |  Процесс завершён   |
  |  (ресурсы освобождены)|     |  (ресурсы освобождены)|
  +---------------------+       +---------------------+
```
# Память
### Resident Memory и поведение при fork()
#### Механизм Copy-On-Write (COW):
1. Изначально оба процесса используют одни и те же физические страницы памяти
2. При записи в любую страницу:
   * Ядро создаёт копию страницы
   * Модифицирующий процесс получает свою копию
3. Read-only доступ не вызывает копирования

```
Родительский процесс:
[Code][Data][Heap][Stack][Shared Libs]
```
```
Родительский процесс:       Дочерний процесс:
[Code][Data][Heap][Stack]   [Code][Data][Heap][Stack]
[Shared Libs] (общие)       [Shared Libs] (общие)
```

# fork vs thread
| Критерий                | `fork()` (Процессы)                                   | Потоки (Threads)                                  |
|-------------------------|-------------------------------------------------------|---------------------------------------------------|
| **Изоляция**            | ✅ Полная (отдельное адресное пространство)            | ❌ Нет (общая память)                              |
| **Производительность**  | ⚠ Медленнее (копирование структур ядра)               | ✅ Быстрее (только новый стек + TCB)               |
| **Память**              | ⚠ Больше (Copy-On-Write, но экономит при read-only)   | ✅ Меньше (~8MB стек на поток)                     |
| **Безопасность**        | ✅ Выше (падение процесса не влияет на другие)         | ❌ Ниже (падение потока = крах всего процесса)     |
| **Синхронизация**       | 🔄 Требуется IPC (pipe, shm и др.)                    | 🔄 Требуются мьютексы/атомарные операции          |
| **Использование CPU**   | 🔄 Ядро планирует независимо                          | ✅ Лучше для CPU-bound задач (меньше переключений) |
| **Отладка**             | 🔄 Проще (изолированные процессы)                     | ❌ Сложнее (гонки данных, deadlocks)               |
| **Типичное применение** | Веб-серверы (nginx), изолированные задачи             | Вычисления (OpenMP), высоконагруженные сервисы    |


# Интересное
### Почему в cat /proc/uptime 2 значения
* Общее время работы системы
* Суммарное время "простоя" (idle_time) всех CPU (также в секундах).
### cat /proc/loadavg 5 значений
| Позиция | Значение  | Описание                                                                 | Как интерпретировать                                                                 |
|---------|----------|-------------------------------------------------------------------------|-------------------------------------------------------------------------------------|
| **1**   | `1.25`   | Средняя нагрузка за **1 минуту** (процессы в R+D состояниях)            | • `>1.00` на 1 ядре = перегрузка<br>• Делите на число ядер для многопроцессорных систем |
| **2**   | `0.95`   | Средняя нагрузка за **5 минут**                                         | Показывает тренд (если растёт — нагрузка увеличивается)                              |
| **3**   | `0.88`   | Средняя нагрузка за **15 минут**                                        | Долгосрочный тренд                                                                  |
| **4**   | `3/456`  | Активные/общие процессы:<br>• `3` — запущенные прямо сейчас (R)<br>• `456` — всего процессов/потоков | • `R > ядер CPU` = очередь<br>• Высокий `456` = много фоновых задач                   |
| **5**   | `7890`   | Последний использованный PID                                            | Резкие скачки = массовый запуск процессов (возможна атака)                          |


### **Сравнение файловых дескрипторов (FD) и inode (ID файлов)**

| Характеристика          | Файловый дескриптор (FD)                          | Inode (ID файла)                      |
|-------------------------|------------------------------------------------|---------------------------------------|
| **Природа**             | Временный идентификатор открытого файла в процессе | Постоянный идентификатор файла в ФС   |
| **Область видимости**   | Локальный для процесса                         | Глобальный в файловой системе         |
| **Уникальность**        | Уникален только внутри процесса                | Уникален в пределах ФС                |
| **Время жизни**         | От `open()` до `close()`                       | От создания до удаления файла         |
| **Тип значения**        | Малое целое число (0, 1, 2, ...)              | Большое целое число                   |
| **Повторное использование** | Да (после закрытия)                        | Нет                                   |
| **Содержит**            | Ссылку на открытый файл/ресурс                | Метаданные файла                      |
| **Пример получения**    | `fd = open("file.txt")` → 3                   | `ls -i file.txt` → 123456             |
| **Типичное использование** | Операции ввода-вывода (`read`, `write`)    | Поиск и управление файлами            |
| **Где хранится**        | В памяти процесса (таблица FD)                | На диске (структура ФС)               |
| **Максимальное количество** | Ограничено `ulimit -n` и `fs.file-max`    | Зависит от ФС (обычно миллионы)       |
