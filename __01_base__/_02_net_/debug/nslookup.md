# Утилита `nslookup` для диагностики DNS

`nslookup` — это утилита для работы с DNS, которая позволяет выполнять запросы к DNS-серверам вручную. Она широко используется для проверки доступности доменных имен и их соответствующих IP-адресов.

---

## **Основной синтаксис**
```sh
nslookup <домен> [DNS-сервер]
```
Где:
- `<домен>` — запрашиваемый домен.
- `[DNS-сервер]` (необязательно) — DNS-сервер, к которому выполняется запрос.

---

## **Примеры использования**

### **1. Запрос IP-адреса домена (A-запись)**
```sh
nslookup example.com
```
**Вывод:**
```sh
Server:  google-public-dns-a.google.com
Address:  8.8.8.8

Non-authoritative answer:
Name:    example.com
Address:  93.184.216.34
```
🔹 **Значение**: Отображает IP-адрес домена.

---

### **2. Запрос к конкретному DNS-серверу**
```sh
nslookup example.com 8.8.8.8
```
🔹 **Значение**: Запрашивает информацию у Google DNS (8.8.8.8).

---

### **3. Получение MX-записей (почтовые серверы)**
```sh
nslookup -query=MX example.com
```
🔹 **Значение**: Отображает почтовые серверы домена.

---

### **4. Получение NS-записей (серверов имен)**
```sh
nslookup -query=NS example.com
```
🔹 **Значение**: Показывает DNS-серверы, обслуживающие домен.

---

### **5. Проверка TXT-записей (например, SPF, DKIM)**
```sh
nslookup -query=TXT example.com
```
🔹 **Значение**: Отображает текстовые записи, используемые для различных целей (например, email-аутентификации).

---

### **6. Интерактивный режим `nslookup`**
```sh
nslookup
> set type=MX
> example.com
```
🔹 **Значение**: Позволяет вручную менять параметры запроса, например, тип записей (A, MX, NS, TXT и т. д.).

---

## **Вывод**
`nslookup` — удобный инструмент для проверки DNS-записей, особенно если нужно быстро получить информацию о домене. Однако для более детального анализа лучше использовать `dig`. 🚀