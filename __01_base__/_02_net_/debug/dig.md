
# Утилита `dig` (Domain Information Groper)

`dig` — это утилита для диагностики DNS (Domain Name System). Она используется для запроса информации о доменных именах, IP-адресах и других DNS-записях.

---

## **Основной синтаксис**

```sh
dig [опции] <домен> [тип_записи]
```

Где:
- `<домен>` — запрашиваемый домен (например, `example.com`).
- `[тип_записи]` — тип DNS-записи (A, MX, NS, TXT и т. д.).
- `[опции]` — дополнительные параметры (например, `+short` для краткого вывода).

---

## **Примеры использования**

### **1. Запрос A-записи (IP-адреса домена)**
```sh
dig example.com A
```
**Вывод:**
```sh
;; ANSWER SECTION:
example.com.    3600    IN    A    93.184.216.34
```
🔹 **Значение**: `A`-запись указывает IPv4-адрес сервера.

---

### **2. Запрос AAAA-записи (IPv6-адреса)**
```sh
dig example.com AAAA
```
Если у домена есть IPv6-адрес, он появится в ответе.

---

### **3. Запрос MX-записей (почтовых серверов)**
```sh
dig example.com MX
```
**Вывод:**
```sh
;; ANSWER SECTION:
example.com.    3600    IN    MX    10 mail.example.com.
```
🔹 **Значение**: Почтовые серверы домена.

---

### **4. Запрос NS-записей (серверов имен)**
```sh
dig example.com NS
```
**Вывод:**
```sh
;; ANSWER SECTION:
example.com.    3600    IN    NS    ns1.example.com.
example.com.    3600    IN    NS    ns2.example.com.
```
🔹 **Значение**: Список DNS-серверов, ответственных за домен.

---

### **5. Запрос TXT-записей (текстовые записи, SPF, DKIM, etc.)**
```sh
dig example.com TXT
```
**Вывод:**
```sh
;; ANSWER SECTION:
example.com.    3600    IN    TXT    "v=spf1 include:_spf.google.com ~all"
```
🔹 **Значение**: Часто используется для SPF, DKIM и других проверок.

---

### **6. Рекурсивный трассинг (опция `+trace`)**
```sh
dig example.com +trace
```
🔹 **Значение**: Показывает полный путь разрешения имени от корневых серверов.

---

### **7. Запрос к конкретному DNS-серверу**
```sh
dig @8.8.8.8 example.com A
```
🔹 **Значение**: Запрашивает A-запись у Google DNS (8.8.8.8).

---

### **8. Краткий вывод (`+short`)**
```sh
dig example.com A +short
```
**Вывод:**
```sh
93.184.216.34
```
🔹 **Значение**: Показывает только IP-адрес без лишней информации.

---

## **Вывод**
`dig` — мощная утилита для диагностики DNS-записей, проверки доступности доменов и выявления проблем с DNS. 🚀