
## TTY
tty* это виртуальные консоли, ttyS — последовательные порты. /dev/pts/* это псевдотерминалы. 
В обычных терминалах есть ядро и есть процесс, работающий с этим терминалом. 
Процесс читает/пишет данные в /dev/tty*, а ядро помещает туда ввод с клавиатуры и выводит данные на экран. 
В случае с псевдотерминалом всё, что читает/пишет процесс в /dev/pts/* обрабатывается другим процессом, 
например sshd или xterm. Ядро просто передаёт данные от одного процесса другому.

## Получить pid и выполнить команду
```shell
#  | Получить строку с именем
#              | Вывести значения в колонку (список)
#                                 | Задать имя переменной {} и выполнить команду echo 
ps | grep s | awk '{print $1}' | xargs -I {} echo {}
```

## ps -aux (BSD)
```shell
USER         PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND
root           1  0.0  0.1 167492 10512 ?        Ss   Mar19   1:30 /sbin/init
root           2  0.0  0.0      0     0 ?        S    Mar19   0:00 [kthreadd]
root           3  0.0  0.0      0     0 ?        I<   Mar19   0:00 [rcu_gp]
```
## ps -ax
```shell
    PID TTY      STAT   TIME COMMAND
      1 ?        Ss     1:30 /sbin/init
      2 ?        S      0:00 [kthreadd]
      3 ?        I<     0:00 [rcu_gp]
```


