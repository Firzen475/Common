# Утилита `pmap` в Linux: анализ использования памяти процессами

## 📌 Основное назначение
`pmap` показывает распределение памяти процесса, включая:
- Сегменты кода, данных, стека
- Динамические библиотеки (`*.so`)
- Shared memory и анонимные отображения (`[anon]`)

## 🛠 Ключевые опции

| Опция | Описание                                         |
|-------|--------------------------------------------------|
| -x	   | Расширенный вывод (RSS, Dirty, Swap)             |
| -d	   | Шестнадцатеричные адреса                         |
| -q	   | Только сводка (без детализации)                  |
| -XX   | 	Максимальная детализация (если поддерживается)  |

## 2. Подробный отчёт (-x)
```bash
pmap -x <PID>
```
```
Address           Kbytes     RSS   Dirty Mode  Mapping
0000555555554000       4       4       0 r-x-- my_program
0000555555755000       4       4       4 r---- my_program
0000555555756000       4       4       4 rw--- my_program
00007ffff7a00000    1800    1500       0 r-x-- libc.so.6
...
total kB          123456   45678    1234
```
### Заголовки столбцов
* Kbytes — размер области памяти в килобайтах.
* RSS (Resident Set Size) — реально используемая RAM.
* Dirty — изменённые, но не сброшенные на диск страницы.
* Swap — сколько памяти вытеснено в своп (если используется -x).
### Интерпретация режимов (Mode)
* `r----`: Read-only (данные, константы).
* `r-x--`: Read+Execute (исполняемый код).
* `rw---`: Read+Write (изменяемые данные).
* `rw-s-`: Shared (разделяемая память между процессами).
* `rw-p-`: Private (копия при записи, например, mmap файлов).

